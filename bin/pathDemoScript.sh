#!/bin/bash

# Test script can be run anywhere b/c I added the ~/bin path to the 
# $PATH env in a ~/.bashrc file. See the export entry there.
# Also see the current path setup with $ echo $PATH

echo "Test script setupReadme.sh @"
echo `date`
